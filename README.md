# Algorithm
알고리즘 코드 정리
<br> </br>
## Commit Convention

**commit message:** ps/`Algorithm Type`/`Problem name`: `refer` || `solve` (date)

`refer` : 해설 참조  
`solve` : 해결  
`refer && solve` : 해설 참조 후 해결
